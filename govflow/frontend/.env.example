VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
# Only needed if the backend has API_KEY_REQUIRED=true set -- leave blank
# for local dev (the backend's auth placeholder defaults to disabled).
VITE_API_KEY=
